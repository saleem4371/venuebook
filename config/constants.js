/**
 * /config/constants.js
 *
 * Platform-wide branding constants. venuebook.in is the ONLY valid brand
 * string — lowercase v, lowercase b, lowercase .in. Never hardcode
 * "VenueBook", "Venue Book", "Venuebook", or "VB" anywhere; import APP_NAME
 * (or APP_NAME_TITLE_CASE for the rare spot that legitimately needs a
 * legal/registered-entity form) instead.
 */

export const APP_NAME = "venuebook.in";
